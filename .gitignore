# Keep in sync with .yamllint's ignore: block for the same generated paths.
collections/
roles/
*.retry
.vault_pass
.lintvenv/
.venv/
containerlab/clab-*/
containerlab/*.annotations.json
__pycache__/
*.pyc
inventory/anta_inventory.generated.yml
# Backstop only -- eos_designs_documentation.enable/eos_cli_config_gen_documentation.enable
# are set false in group_vars/FABRIC/fabric_variables.yml, so this shouldn't regenerate.
inventory/documentation/
# inventory/intended/ is deliberately NOT listed here -- it's committed
# AVD output (see docs/LLD.md), just excluded from .yamllint's lint pass.
